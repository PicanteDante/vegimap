
The api helps handle requests.
Think of them as function that you call from the client to the server.
The server then does something and returns a response to the client.

In order to use the api, you need to know the endpoint of the api.

also, here's a template for requests:
fetch("/api/endpoint", {
    method: "POST",
    headers: {
        "Content-Type": "application/json"
    },
    body: JSON.stringify({
        //data
    })
}).then(res => {
    if (res.ok) {
        return res.json();
    } else {
        throw new Error();
    }
}).then(data => {
    // do something with the data
}).catch(err => {
    // do something with the error
});


Current endpoints and their status:
    /api/register           GOOD
    /api/login              GOOD
    /api/markers/get_id     GOOD
    /api/markers/add        NOT WORKING
    /api/markers/list_own   GOOD
    /api/markers/list       GOOD
    /api/markers/get        NOT WORKING
    /api/markers/upvote     NOT WORKING
    /api/markers/downvote   NOT WORKING
    /api/markers/delete     NOT WORKING
    /api/top_users          GOOD

Documentation:
    /api/register
        POST
        data: {
            username: string,
            password: string,
            email: string
        }
        response: {
            message: string
            user_id: int
        }
    /api/login
        POST
        data: {
            username: string,
            password: string
        }
        response: {
            success: boolean,
            message: string,
            user_id: int
        }
    /api/markers/get_id
        POST
        data: {
            username: string
        }
        response: {
            user_id: int
        }