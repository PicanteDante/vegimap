<div class="content">
    <a class="avatar" href="{{profileUrl}}">
        <img src="{{pfpUrl}}">
    </a>        
    <div class="rank-info"> 
        <a class="name" href="{{profileUrl}}">{{username}}</a>
    </div>
    <div class="points">{{profilePoints}}</div>
</div>