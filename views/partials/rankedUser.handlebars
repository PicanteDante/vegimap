<div class="content">
    <a class="avatar" href="{{username}}">
        <img src="{{pfpUrl}}">
    </a>        
    <div class="rank-info"> 
        <a class="name" href="{{username}}">{{username}}</a>
    </div>
    <div class="points">{{profilePoints}}</div>
</div>